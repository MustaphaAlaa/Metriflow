FROM mcr.microsoft.com/dotnet/sdk:8.0

WORKDIR /app

# Copy csproj files first (for restore caching)
COPY src/Core/Metriflow.Application/Metriflow.Application.csproj src/Core/Metriflow.Application/
COPY src/Infrastructure/Metriflow.Messages/Metriflow.Messages.csproj src/Infrastructure/Metriflow.Messages/
COPY src/Infrastructure/Redis/Redis.csproj src/Infrastructure/Redis/
COPY src/Execution/Metriflow.Matcher.Worker/Metriflow.Matcher.Worker.csproj src/Execution/Metriflow.Matcher.Worker/

RUN dotnet restore src/Execution/Metriflow.Matcher.Worker/Metriflow.Matcher.Worker.csproj

# Copy the rest of the source
COPY src ./src

WORKDIR /app/src/Execution/Metriflow.Matcher.Worker

# Dev defaults
ENV ASPNETCORE_ENVIRONMENT=Development
ENV DOTNET_USE_POLLING_FILE_WATCHER=true

# Run directly from source
CMD ["dotnet", "run"]

